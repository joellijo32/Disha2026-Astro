---
import DitherCanvas from './ThreeCanvasImpl.jsx';
const REAL_EVENTS_DATA = [
  { id: 1, title: 'MAIN EVENTS', eventsList: ['Face of Disha', 'MX-Techie', 'MX-Disha', 'Fashion Show', 'Street Play', 'Disha Idol', 'Just Dance', 'Wall Painting'] },
  { id: 2, title: 'TECHNICAL', eventsList: ['Ideathon', 'Quiz', 'Technical Essay', 'Prompt Writing', 'Speed Typing'] },
  { id: 3, title: 'E-GAMES', eventsList: ['E-Football', 'BGMI', 'Valorant', 'Mini Militia', 'Free Fire', 'FIFA', 'COD'] },
  { id: 4, title: 'SPORTS', eventsList: ['Football', 'Cricket', 'Volleyball', 'Tug of War', 'Badminton', 'Table Tennis', 'Chess', 'Carroms', 'Cycle Slow Race', 'Basketball', 'Arm Wrestling', 'Super Over (W)', 'Penalty Shoot (W)', 'Mr. Ponjikkara'] },
  { id: 5, title: 'ATHLETICS', eventsList: ['100M Race (M/W)', 'Shot Put (M/W)', 'High Jump', 'Long Jump', '400 M Relay (M/W)', 'Brisk Walk 800M (M/W)', '200M Race (M/W)', 'Biathlon'] },
  { id: 6, title: 'LITERARY', eventsList: ['Story Writing (Eng)', 'Story Writing (Mal)', 'Story Writing (Hin)', 'Versification (Eng)', 'Versification (Mal)', 'Versification (Hin)', 'Jingle Writing (Eng)', 'Jingle Writing (Mal)', 'Book Review', 'Film Review', 'Recitation (Eng)', 'Recitation (Mal)', 'Recitation (Hin)', 'Short Story (Eng)', 'Short Story (Mal)', 'Caption Writing (Eng)', 'Caption Writing (Mal)', 'Extempore (Eng)', 'Extempore (Mal)', 'Extempore (Hin)'] },
  { id: 7, title: 'MUSICAL', eventsList: ['Classical Music', 'Light Music', 'Western (Duet)', 'Bands Battle', 'Nadanpattu', 'Unplugged', 'Mappilappattu'] },
  { id: 8, title: 'DANCE', eventsList: ['Adapt Tunes', 'Prepared Duo', 'Prepared Solo'] },
  { id: 9, title: 'THEATRICAL', eventsList: ['Street Play', 'Mono Act', 'Mimicry', 'Stand-up Comedy', 'Skit'] },
  { id: 10, title: 'ART', eventsList: ['Pencil Drawing', 'Water Colouring', 'Cartoon', 'Digital Painting', 'Face Painting', 'Doodle Art', 'Minimal Poster', 'Oil Painting', 'Clay Modelling', 'Origami', 'Bottle Art', 'Calligraphy'] },
  { id: 11, title: 'MISC', eventsList: ['Troll Making', 'Rubix Cube', 'Short Film', 'Department Magazine', 'Mannequin', 'Trailer Making', 'Reel Making', 'Business (Naptol)', 'Channel Surfing', 'Guess Who?', 'Treasure Hunt', 'Open Debate', 'Jam', 'Integration Bee', 'Spot Commentary', 'Shot Story', 'Picture Perception', 'Theme-Based Photography', 'Movie Recreation'] },
];
---
<section id="events" class="relative w-full bg-black z-20" style="touch-action: pan-y;">
  <div class="absolute inset-0 z-0 h-full w-full pointer-events-auto">
    <div class="sticky top-0 w-full h-[100dvh] overflow-hidden bg-black">
      <div class="absolute inset-0 w-full h-full">
        <DitherCanvas
          client:load
          waveColor={[0.45, 0.0, 0.2]}
          disableAnimation={false}
          enableMouseInteraction={true}
          mouseRadius={0.6}
          colorNum={4}
          waveAmplitude={0.3}
          waveFrequency={3}
          waveSpeed={0.05}
        />
      </div>
      <div class="absolute inset-0 bg-black/30 pointer-events-none"></div>
      <div class="absolute inset-0 bg-[radial-gradient(circle_at_center,_var(--tw-gradient-stops))] from-[#8A003B]/30 via-transparent to-transparent opacity-80 pointer-events-none"></div>
      <div class="absolute top-0 left-0 w-full h-64 md:h-96 bg-gradient-to-b from-black via-black/60 to-transparent z-10 pointer-events-none"></div>
    </div>
  </div>
  <div id="events-scroll-section" class="sticky top-0 z-10 w-full h-[100dvh] flex items-center overflow-hidden pointer-events-none">
    <div class="absolute top-10 md:top-14 left-0 z-50 w-full flex justify-center pointer-events-none px-4">
      <h2 class="flex flex-col md:flex-row items-center md:items-baseline gap-1 md:gap-5">
        <span class="text-[#DE005F] -mt-4 font-['Sedgwick_Ave_Display',_cursive] font-extrabold tracking-[-0.05em] text-6xl md:text-9xl relative [text-shadow:0_0_15px_#DE005F]">
          EVENTS
        </span>
      </h2>
    </div>
    <div id="events-cards-wrapper" class="flex items-center h-full w-max px-[5vw] gap-6 md:gap-12 mt-28 md:mt-32">
      {REAL_EVENTS_DATA.map((event) => (
        <div
          key={event.id}
          class="event-card pointer-events-auto w-[85vw] sm:w-[60vw] md:w-[45vw] lg:w-[32vw] h-[70vh] flex flex-col bg-black/60 border border-white/10 rounded-none p-6 md:p-10 shrink-0 shadow-none md:shadow-[0_0_50px_rgba(0,0,0,0.5)] relative overflow-hidden"
        >
          <div class="shrink-0 mb-6 w-full flex justify-center">
            <h3 class="text-2xl sm:text-3xl md:text-4xl font-black uppercase tracking-wider text-[#DE005F] break-words text-center font-bohme [text-shadow:0_0_15px_#DE005F]">
              {event.title}
            </h3>
          </div>
          <div class="flex-grow overflow-y-auto overflow-x-hidden custom-scrollbar pr-2 relative w-full">
            <div class="relative flex items-center justify-center gap-3 mb-6 sticky top-0 z-20 pt-2 pb-6 -mt-2 w-full">
              <div class="absolute inset-0 -mx-6 bg-gradient-to-b from-black/90 via-black/50 to-transparent pointer-events-none"></div>
              <span class="relative z-10 h-px flex-grow bg-gradient-to-r from-transparent to-[#DE005F]"></span>
              <span class="relative z-10 h-px flex-grow bg-gradient-to-l from-transparent to-[#DE005F]/50"></span>
            </div>
            <div class="flex flex-col w-full">
              {event.eventsList.map((item, index) => (
                <div key={index} class="group flex items-start gap-4 py-3 md:py-4 border-b border-white/10 hover:border-white/30 transition-colors w-full">
                  <span class="text-[#DE005F] font-black text-sm md:text-base w-5 shrink-0 mt-0.5 font-myriad">
                    {(index + 1).toString().padStart(2, '0')}
                  </span>
                  <span class="text-white/90 group-hover:text-white font-bold tracking-wide text-sm sm:text-base md:text-lg flex-1 break-words whitespace-normal leading-snug font-myriad">
                    {item}
                  </span>
                </div>
              ))}
            </div>
          </div>
        </div>
      ))}
    </div>
  </div>
  <div id="events-spacer" class="w-full pointer-events-none"></div>
</section>

<style>
  /* Promote wrapper to its own compositor layer once, avoid promote-3d overhead */
  #events-cards-wrapper {
    will-change: transform;
    /* GPU layer without preserve-3d overhead */
    transform: translateZ(0);
  }
</style>

<script>
(function () {
  'use strict';

  var wrapper = document.getElementById('events-cards-wrapper');
  var spacer  = document.getElementById('events-spacer');
  if (!wrapper || !spacer) return;

  /* ─── state ─────────────────────────────────────────────────── */
  var sectionTop  = 0;   // px from document top — set once per layout
  var maxScroll   = 0;   // total horizontal travel
  var displayX    = 0;   // what's currently on screen
  var rafId       = null;
  var leadIn      = 0;   // px of vertical scroll to absorb before horizontal motion starts

  /* touch state */
  var touchActive = false;
  var touchStartX = 0;
  var touchStartY = 0;
  var touchBaseX  = 0;
  var prevTouchX  = 0;
  var prevTouchT  = 0;
  var velX        = 0;   // px / ms

  /* ─── helpers ────────────────────────────────────────────────── */
  function clamp(v, lo, hi) { return v < lo ? lo : v > hi ? hi : v; }

  /* Read the section's document-relative top accurately. offsetTop chains
     break across position:sticky/absolute ancestors (this layout has both),
     silently producing a wrong value. getBoundingClientRect is reliable
     regardless of positioning context; called only in setup(), not on
     every scroll, so the single reflow it costs is negligible. */
  function getSectionTop() {
    var section = wrapper.closest('#events');
    if (!section) return 0;
    return section.getBoundingClientRect().top + window.scrollY;
  }

  function applyTransform(x) {
    /* Only one CSS property on a composited layer — no layout, no paint. */
    wrapper.style.transform = 'translate3d(' + (-x) + 'px,0,0)';
  }

  /* ─── setup (called on load + resize, NOT on scroll) ─────────── */
  function setup() {
    maxScroll  = Math.max(0, wrapper.scrollWidth - window.innerWidth);
    sectionTop = getSectionTop();

    /* Hold position for the first half of the section's pinned duration —
       lets the user finish reading the heading before horizontal motion
       begins. */
    leadIn = window.innerHeight * 0.675;

    spacer.style.height = (maxScroll + leadIn) + 'px';

    /* Snap display to current scroll position immediately. */
    displayX = clamp(window.scrollY - sectionTop - leadIn, 0, maxScroll);
    applyTransform(displayX);
  }

  /* ─── RAF loop — only runs when there's work to do ───────────── */
  var FRICTION = 0.88; // velocity multiplier per 16.67 ms frame

  function loop(now) {
    rafId = null;

    if (touchActive) return; /* Touch move drives applyTransform directly */

    /* Apply fling momentum (mobile swipe) */
    if (Math.abs(velX) > 0.05) {
      displayX = clamp(displayX + velX * 16.67, 0, maxScroll);
      velX *= FRICTION;
      applyTransform(displayX);
      /* Sync page scroll so the two stay in step */
      window.scrollTo({ top: sectionTop + leadIn + displayX, behavior: 'instant' });
      rafId = requestAnimationFrame(loop);
    }
  }

  function startLoop() {
    if (!rafId) rafId = requestAnimationFrame(loop);
  }

  /* ─── Scroll handler — read-only, batched via rAF ────────────── */
  var scrollQueued = false;
  function onScroll() {
    if (touchActive || scrollQueued) return;
    scrollQueued = true;
    requestAnimationFrame(function () {
      scrollQueued = false;
      var target = clamp(window.scrollY - sectionTop - leadIn, 0, maxScroll);
      if (target !== displayX) {
        displayX = target;
        applyTransform(displayX);
      }
    });
  }

  /* ─── Touch handlers ─────────────────────────────────────────── */
  function onTouchStart(e) {
    var t = e.touches[0];
    touchStartX  = t.clientX;
    touchStartY  = t.clientY;
    prevTouchX   = t.clientX;
    prevTouchT   = performance.now();
    touchBaseX   = displayX;
    velX         = 0;
    touchActive  = false; /* determined after first move */
    if (rafId) { cancelAnimationFrame(rafId); rafId = null; }
  }

  function onTouchMove(e) {
    var t   = e.touches[0];
    var dx  = touchStartX - t.clientX;
    var dy  = touchStartY - t.clientY;

    if (!touchActive) {
      /* Decide axis on first significant movement */
      if (Math.abs(dx) < 4 && Math.abs(dy) < 4) return;
      if (Math.abs(dy) >= Math.abs(dx)) return; /* vertical — let browser handle */
      touchActive = true;
    }

    /* Prevent native vertical scroll only when we own this gesture */
    if (e.cancelable) e.preventDefault();

    var now  = performance.now();
    var dt   = now - prevTouchT;
    if (dt > 0) velX = (prevTouchX - t.clientX) / dt; /* px / ms */
    prevTouchX = t.clientX;
    prevTouchT = now;

    displayX = clamp(touchBaseX + dx, 0, maxScroll);
    applyTransform(displayX);
  }

  function onTouchEnd() {
    if (!touchActive) return;
    touchActive = false;

    /* Sync page scroll then kick off momentum */
    window.scrollTo({ top: sectionTop + leadIn + displayX, behavior: 'instant' });

    /* Convert px/ms → px/frame (≈16.67 ms) and apply initial momentum */
    if (Math.abs(velX) > 0.05) {
      velX *= 16.67; /* scale to per-frame units */
      startLoop();
    }
  }

  /* ─── Wire up ────────────────────────────────────────────────── */
  var section = (wrapper.parentElement && wrapper.parentElement.parentElement);

  window.addEventListener('scroll', onScroll, { passive: true });

  if (section) {
    section.addEventListener('touchstart', onTouchStart, { passive: true });
    section.addEventListener('touchmove',  onTouchMove,  { passive: false });
    section.addEventListener('touchend',   onTouchEnd,   { passive: true });
    section.addEventListener('touchcancel',onTouchEnd,   { passive: true });
  }

  /* Debounced resize — avoid hammering setup() */
  var resizeTimer;
  window.addEventListener('resize', function () {
    clearTimeout(resizeTimer);
    resizeTimer = setTimeout(setup, 150);
  }, { passive: true });

  /* Run setup after layout is stable */
  if (document.readyState === 'complete') {
    setup();
  } else {
    window.addEventListener('load', setup, { once: true });
  }
})();
</script>
